# Init


From ESLint [docs](https://github.com/eslint/eslint#installation-and-usage).

```sh
$ eslint --init
```

Result:

- `.eslintrc`
    ```json
    {
      "rules": {
        "semi": ["error", "always"],
        "quotes": ["error", "double"]
      }
    }
    ```
